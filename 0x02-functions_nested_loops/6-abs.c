#include "main.h"

/**
 * _abs - checks number
 * @int: number to check
 * Return: n if positive, -n if negative, 0 otherwise
 */

int _abs(int)
{
	int n, m;

	if (n > 0)
	{
		printf("%d\n", n);
	}
	else if (n < 0)
	{
		m = -n;
		printf("%d\n", m);
	}
	else
	{
		printf("%d\n", 0);
	}
}
